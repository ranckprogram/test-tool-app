{
  "name": "testtoolapp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "electron . --enable-logging",
    "start": "electron .",
    "pack": "electron-packager . myClient --win --out ../myClient --arch=x64 --app-version=0.0.1 --electron-version=5.0.2"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.5.5"
  },
  "dependencies": {
    "excel-parser": "^0.2.2",
    "iconv-lite": "^0.4.24",
    "node-xlsx": "^0.15.0",
    "readline": "^1.3.0"
  }
}
